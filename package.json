{
  "name": "treeeditor",
  "version": "1.0.0",
  "description": "",
  "main": "out.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild frontend/src/main.ts --bundle --outfile=frontend/dist/out.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.7",
    "esbuild": "0.24.0"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
